# Control Plane v1.0 Dependencies
# Python version constraint lives in pyproject.toml (requires-python = ">=3.10")
# Do NOT add "python>=X.Y" here — it is not a valid pip package name.

# FastAPI and ASGI server
fastapi>=0.136.0
uvicorn[standard]>=0.24.0
pydantic>=2.13.1

# CLI
click>=8.1.0

# TUI — Rich Terminal UI (used by CLI, install_ui, tune_signedai)
rich>=13.7.0
pyfiglet>=1.0.2

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-timeout>=2.3.1  # Kill tests hanging >30s — prevents WSL terminal zombie processes
pytest-cov>=4.1.0      # Coverage reporting (used in CI: --cov=microservices)
httpx>=0.25.0  # For TestClient
numpy>=1.24.0  # Required by microservices/vector-search/conftest.py (FakeFaissIndex)

# Cryptography - required by rct_control_plane/signed_execution.py (ED25519 signing)
cryptography>=41.0.0

# Optional: Production deployment
gunicorn>=21.2.0  # For production WSGI serving

# ── Dashboard deps (internal only — not published to public repo) ────────────
# streamlit, plotly, pandas, watchdog, streamlit-autorefresh
# are development/dashboard tools and are excluded from the public SDK.
# Install them separately if you run the internal Benchmark Command Center.
